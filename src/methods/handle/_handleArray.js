/** ----------------------------------------
    Handle Array
 ---------------------------------------- */

function _handleArray(array) {
    return array.match(/"(.*?)"/g);
}

/** ----------------------------------------
    Exports
 ---------------------------------------- */

export default _handleArray;